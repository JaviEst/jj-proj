This is a minimal frontend placeholder used to validate Docker builds in CI.

For a real Angular UI, replace this folder with your Angular project while keeping:
- `npm run build -- --configuration production`
- build output under `frontend/dist`
